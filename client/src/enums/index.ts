export enum LoadingStates {
  IDLE = "idle",
  LOADING = "loading",
  SUCCESS = "success",
  FAILURE = "failure",
}

export enum UserRoles {
  ADMIN = "ADMIN",
  EMPLOYEE = "EMPLOYEE",
  PATRON = "PATRON",
}
